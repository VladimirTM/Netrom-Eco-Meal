@page "/account/forgot-password"
@layout EmptyLayout
@rendermode InteractiveServer
@using Netrom_Eco_Meal.Controllers
@inject AuthController AuthController

<PageTitle>Forgot password — Eco Meal</PageTitle>

<div class="login-page">
    <div class="login-card">
        <div class="login-brand">
            <img src="logo.png" alt="Eco Meal" class="login-logo"/>
            <div>
                <div class="login-brand-name">Eco Meal</div>
                <div class="login-brand-sub">Surplus food, rescued</div>
            </div>
        </div>

        <h1 class="login-heading">Forgot password</h1>

        @if (_submitted)
        {
            <div class="alert alert-success py-2 small mb-3" role="alert">
                If an account exists for that email, we've sent a link to reset your password.
            </div>
        }
        else
        {
            <p class="text-muted small mb-3">Enter your account email and we'll send you a link to reset your password.</p>
            <div class="mb-4">
                <label class="form-label fw-semibold" for="forgot-email">Email</label>
                <input id="forgot-email" class="form-control" type="email" @bind="_email"
                       placeholder="you@example.com" autocomplete="email"/>
            </div>
            <button class="btn btn-primary w-100 py-2 fw-semibold" disabled="@_busy" @onclick="SubmitAsync">
                @(_busy ? "Sending…" : "Send reset link")
            </button>
        }

        <p class="login-footer-link">
            <a href="/account/login">Back to sign in</a>
        </p>
    </div>
</div>

@code {
    private string _email = "";
    private bool _busy;
    private bool _submitted;

    private async Task SubmitAsync()
    {
        if (string.IsNullOrWhiteSpace(_email))
            return;

        _busy = true;
        await AuthController.RequestPasswordResetAsync(_email.Trim());
        _busy = false;
        _submitted = true;
    }
}
